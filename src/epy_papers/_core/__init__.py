"""Core infrastructure for epy_papers.

Sub-packages:
    _packaging — build/release tooling (installer sources, dev scripts,
        icon assets). Not part of the runtime application; excluded from
        the distributed wheel (see ``pyproject.toml``).
"""
