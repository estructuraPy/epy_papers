"""Bundled package configuration: static assets and reference data.

Sub-packages:
    _assets — CSL styles, LaTeX classes, branding images, MathJax bundle,
        reference DOCX templates, theme layouts and the manual Markdown.
    _data   — the journal profile catalog (``journals.json``).
"""
